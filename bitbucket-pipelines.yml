image: php:7.2

pipelines:
  branches:
    develop:
      - step:
          name: Production
          caches:
            - composer
          script:
            - sudo chmod +x /usr/bin/ssh
            - ssh -T root@dev2.cogniteq.com -p2110
            - git pull
