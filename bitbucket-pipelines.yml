image: atlassian/default-image:latest

pipelines:
  default:
    - step:
        deployment: staging
        script:
          - ssh -T root@dev2.cogniteq.com -p2110
          - eval `ssh-agent -s`
          - ssh-add ~/.ssh/id_rsa
          - ssh-add -l
          - cat ./deploy.sh
          - echo "Deploy step finished"
